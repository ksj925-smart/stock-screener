# SPEC.md

> **이 문서의 출처에 대한 안내**
>
> 리포 전체(git 히스토리 포함)를 검색했지만 원본 `SPEC.md`가 커밋된 적이 없습니다.
> 코드 곳곳에는 `SPEC 3장`, `SPEC 5-1` 같은 참조 주석만 반복해서 남아 있을 뿐,
> 실제 SPEC 문서 자체는 이 저장소에 존재하지 않았습니다.
>
> 이 문서는 그 참조 주석들을 `grep -ril`로 전부 찾아 **역으로 취합·재구성**한
> 것입니다. 따라서:
> - 각 장의 "내용"은 원본 SPEC 문서의 전문이 아니라 **코드 주석에 남은 원칙을
>   인용/요약**한 것입니다. 원본이 따로 있다면 그것을 기준으로 이 문서를
>   교정해 주세요.
> - 코드에서 근거를 찾지 못한 항목(1장·2장·3-1·3-2·5-2·8장의 정확한 하위 구조 등)은
>   **추측하지 않고 "근거 불명 — 확인 필요"**로 표시했습니다.
> - 모든 인용에는 근거가 된 `파일:줄` 을 표기했습니다.

## 목차

1. 근거 불명
2. 근거 불명
3. [판단 유도 금지 및 표시 원칙](#3장--판단-유도-금지-및-표시-원칙)
4. [데이터 출처 및 라이선스](#4장--데이터-출처-및-라이선스)
5. [필터 UX 명세](#5장--필터-ux-명세)
6. [즐겨찾기](#6장--즐겨찾기)
7. [데이터 파이프라인](#7장--데이터-파이프라인)
8. [Phase 1 검증 체크리스트](#8장--phase-1-검증-체크리스트)
- [부록 A — 변경 이력(승인 사항)](#부록-a--변경-이력승인-사항)
- [부록 B — SPEC 번호 없이 남은 심사 대응 이력](#부록-b--spec-번호-없이-검토-반려-이력만-남은-항목-참고)
- [부록 C — 백로그: filters.ts RSI 슬라이더 문구](#부록-c--백로그-filtersts-rsi-슬라이더-문구과매도과열)

---

## 1장, 2장

근거 불명 — 확인 필요. "SPEC 1장" · "SPEC 2장"을 참조하는 코드 주석을 리포 전체에서
찾지 못했습니다.

---

## 3장 — 판단 유도 금지 및 표시 원칙

이 앱은 **"자본시장법상 투자자문업·투자일임업으로 등록되어 있지 않"**습니다
(`src/components/DataFooter.tsx:25`). 따라서 매매 판단을 유도할 수 있는 모든
표현·시각 요소를 배제하는 것이 앱 전체를 관통하는 핵심 원칙입니다.

### 세부 번호 불명 항목 (주석 원문 인용)

**차트** — `src/components/ChartSheet.tsx:20-23`, `src/data/loadSeries.ts:8-9`,
`pipeline/build_series.py:10-11`, `src/App.css:521-524`:

> 볼린저밴드·이동평균선 등 오버레이, 매수/매도 마커, 목표가·지지/저항선,
> "저점·고점·매수 구간" 등 판단 라벨, 특정 구간 색 강조 — 절대 넣지 않는다.
> 허용: 종가 라인 / 날짜 축 / 가격 축 / 기준일 표기. 주석 없는 맨 차트.
> 선 색은 방향성 없는 민트 단색(--acc)만 쓴다 — 등락에 빨강/파랑을 쓰면 색
> 자체가 방향 판단을 유도하기 때문.

이 원칙은 `ChartSheet.tsx` 주석에 **"v1.3 확정"**이라고 명시되어 있었습니다.
→ [부록 A](#부록-a--변경-이력승인-사항)에서 앱인토스 승인에 따라 이 원칙을
**부분 개정**합니다(밴드 선만 예외 허용, 그 외 전부 유지).

**튜토리얼 문구** — `src/components/TutorialSheet.tsx:21-23`:

> 기능 사용법만 설명한다. 투자 판단·추천 표현("이런 조건이 좋아요" 등)과
> 실제 종목명은 절대 넣지 않는다. 예시가 필요하면 가상 이름(A전자)을 쓴다.

**즐겨찾기 카테고리 색** — `src/hooks/useFavorites.ts:37-38` (SPEC 번호는
직접 언급되지 않았으나 동일 원칙의 적용 사례로 판단):

> 빨강/초록/주황은 이 앱에서 등락·손익·매수 신호로 읽히므로 카테고리 색으로
> 쓰지 않는다.

### 3-3 — 자체 점수·순위 금지

`pipeline/build_json.py:11`:

> 자체 점수·순위 필드는 절대 추가하지 말 것 (SPEC 3-3 절대 금지 목록).

### 3-4 — 데이터 출처·유의사항 필수 표기

`src/components/DataFooter.tsx:6-8`:

> 데이터 출처·유의사항 — SPEC 3-4 필수 표기. 문구를 임의로 수정하지 말 것.
> ("참고용" 문장은 2026-07-17 토스 입점 확인 조건으로 추가됨 — SPEC 3-4와 일치)
> (재무 기준 시점 문장은 2026-07-18 사용자 확정으로 추가됨 — 연도는 데이터에서 동적)

표시 문구 원문 (`src/components/DataFooter.tsx:12-29`):

> 본 서비스는 투자 자문이 아닌 정보 제공 목적이며, 모든 지표는 참고용으로
> 제공됩니다. 자본시장법상 투자자문업·투자일임업으로 등록되어 있지 않습니다.
> 특정 종목의 매수·매도를 권유하지 않으며, 투자 판단과 그에 따른 책임은
> 이용자 본인에게 있습니다. 제공 정보의 정확성과 완전성을 보장하지 않습니다.

### 3-1, 3-2

근거 불명 — 확인 필요. 이 번호를 직접 참조하는 코드 주석을 찾지 못했습니다.

### ⚠️ 관찰된 불일치 (판단하지 않고 보고만 함)

`src/lib/filters.ts:51,56`의 RSI 슬라이더 툴팁·눈금은 이미 "과매도" · "과열"
표현을 쓰고 있습니다:

> 보통 30 아래를 과매도, 70 위를 과열이라고 부릅니다. (tip)
> ["0", "30 과매도", "50", "70 과열", "100"] (ticks)

[부록 A](#부록-a--변경-이력승인-사항)의 신규 승인 조건 중 하나가 "'매수신호·
과매수/과매도' 등 매매판단 표현 ... 미포함"인데, 문구가 정확히 겹칩니다.
기존 문구는 "일반적으로 그렇게 부른다"는 객관적 서술이라 이번 승인 이전부터
있던 것으로 보이지만, 신규 지표(RSI20 등) 문구를 쓸 때 이 기존 문구와
일관성을 맞출지(예: "과매도" 대신 순수 수치 설명으로 통일할지) 확인이
필요합니다. 임의로 바꾸지 않았습니다.

---

## 4장 — 데이터 출처 및 라이선스

`pipeline/config.py:1`:

> 공통 설정 — 공공데이터포털 API (SPEC 4장, 라이선스 검증 완료 소스만 사용)

금지 API 목록 — 공공누리 2유형(상업적 이용 금지), `pipeline/config.py:3-4`,
`pipeline/README.md:23-28`:

- 금융위원회_주식배당정보
- 금융위원회_주식발행정보 — 발행주식수 대신 주식시세정보의 `lstgStCnt` 사용,
  시가총액은 `mrktTotAmt` 사용
- 금융위원회_국제거래종목정보
- (패턴) 원천이 한국예탁결제원인 API는 2유형일 가능성이 높음 — 신규 API 추가
  시 이용허락범위를 반드시 먼저 확인하고 보고할 것 (`pipeline/README.md:29`)

데이터 갱신 시점 — `pipeline/fetch_prices.py:3`:

> 공공데이터 갱신은 '기준일자 + 영업일 하루 뒤 13시 이후'이므로(SPEC 4장),
> 오늘부터 최대 10일을 거슬러 올라가며 데이터가 있는 가장 최근 기준일을 찾는다.

---

## 5장 — 필터 UX 명세

`src/lib/filters.ts:25`:

> 슬라이더 명세 (SPEC 5장) — 배당수익률은 라이선스 문제로 제외

### 5-1 — 슬라이더 경계·결측 처리

`src/lib/filters.ts:149`:

> 슬라이더 끝(0/100)은 해당 방향 제한 없음 (SPEC 5-1)

`pipeline/build_json.py:9-10`:

> 결측 처리(SPEC 5-1): 재무 미제출·신규상장 등으로 값이 없으면 null.
> 적자(EPS<=0)면 PER은 null → 프론트의 '적자 회사 빼고 보기' 토글이 걸러낸다.

### 5-2

근거 불명 — 확인 필요.

### 5-3 — 카운트바

`src/components/CountBar.tsx:9,38`:

> 카운트 롤링 애니메이션 (~240ms, SPEC 5-3)
> 하단 고정 카운트바 — 슬라이더 조작 피드백의 핵심 (SPEC 5-3)

관련 컴포넌트(SPEC 번호 명시 없음) — `src/components/FilterSlider.tsx:17`:
양방향 range 슬라이더 + 히스토그램 + 용어 설명 ⓘ.

---

## 6장 — 즐겨찾기

`src/hooks/useFavorites.ts:5,7-10`:

> 즐겨찾기 — localStorage 저장 (SPEC 6장, 토스 로그인 불필요).
> v1.2에서 스키마를 v1(string[])에서 v2(items+categories)로 확장했다.
> - 최대 20개 (신규 담기에만 적용 — 마이그레이션은 유실 없이 전부 보존)
> - 사용자가 만든 카테고리(폴더)로 분류. 카테고리 없이도 동작한다.
> - 순서는 사용자가 정한 그대로 유지하고 자동 정렬하지 않는다.

`src/components/FavList.tsx:28-29`:

> 즐겨찾기 목록 — grip 드래그로 순서 변경(터치·마우스), 카테고리 필터·지정
> (SPEC 6장 + v1.2). 자동 정렬 없음: 순서는 오직 사용자가 정한다.

---

## 7장 — 데이터 파이프라인

### 7-1 — screener.json 스키마

`src/types.ts:1`, `pipeline/build_json.py:1`. 실제 타입 정의는
`src/types.ts`의 `Stock` / `ScreenerMeta` / `ScreenerData` 참고. 용량 절감을
위해 1글자 축약 키(`c`,`n`,`m`,`p`,`r`,`cap`,`rsi`,`pbr`,`per`,`h?`)를 쓰고,
평시 종목은 `h` 필드를 아예 생략한다.

### 7-2 — API 호출 방식

`pipeline/api_client.py:1`:

> 공공데이터포털 공통 호출 유틸 — 페이지네이션 처리 (종목별 개별 호출 금지,
> SPEC 7-2)

`pipeline/fetch_financials.py:1`, `pipeline/price_cache.py:5`:

> 기업재무정보는 분기 1회만 실행 (SPEC 7-2). 종가 캐시는 매일 파이프라인이
> 돌며 자연히 누적된다(개발계정 트래픽으로 충분, SPEC 7-2).

### 세부번호 불명 — RSI(14) 계산·캐시 (7장 소속으로 판단)

- `pipeline/indicators.py:1` — RSI(14) Wilder 평활 방식 계산.
- `pipeline/price_cache.py:1` — 일별 종가 누적 캐시, 30영업일치 유지.
- `pipeline/backfill_prices.py:1,3-4` — RSI 워밍업용 과거 종가 백필(1회성).
  매일 1봉씩만 누적하면 RSI(14)가 14영업일 후에야 나와 출시가 3주 밀리는
  문제를 보완.
- `.github/workflows/screener.yml:1` — 평일 하루 11회 실행, 공공데이터 갱신
  시점(영업일 하루 뒤 13:00 KST 이후) 확보 목적.
- `.gitignore:14` — `data/price_cache.json`, `data/financials.json`은 RSI
  누적·재무 캐시라 GitHub Actions가 커밋해 영속화해야 하므로 gitignore하지
  않는다.

---

## 8장 — Phase 1 검증 체크리스트

`pipeline/config.py:39`, `pipeline/fetch_financials.py:10,62`,
`pipeline/fetch_prices.py:43`, `pipeline/README.md:46-50`에 **"SPEC 8장
Phase1-8"**이라는 동일 표현이 반복됩니다. "Phase1-8"이 (a) "8장의 Phase 1
항목"인지 (b) "Phase 1의 8번째 확인 항목"인지는 **근거 불명 — 확인 필요**
입니다. 다만 실질 체크리스트 내용은 `pipeline/README.md:46-52`에 남아 있어
그대로 인용합니다:

> API 키 수령 후 반드시 검증할 것:
> - [ ] 주식시세정보 실제 응답 필드명 (`srtnCd`, `clpr`, `fltRt`,
>   `mrktTotAmt`, `lstgStCnt`, `mrktCtg`)
> - [ ] 기업재무정보 오퍼레이션/필드명 (`getSummFinaStat_V2`, `enpTcptAmt`,
>   `enpCrtmNpf`, `fnclDcd`)
> - [ ] 결측률 측정 (재무 미제출 종목 수)
> - [ ] screener.json 용량 (목표 ~200KB)
> - [ ] RSI는 캐시가 15영업일 이상 쌓여야 계산됨 — 초기 2~3주는 null

검증 완료 기록 — `pipeline/fetch_financials.py:60-65`:

> API는 '필드 부재(=데이터 구멍)'와 '실제 값 0'을 구분하지 못하고 둘 다
> 문자열 "0"으로 반환한다(SPEC 8장 Phase1-8 전수 확인: KEY_ABSENT/빈문자열
> 전무). 따라서 '연결 순이익이 0이면 별도로 대체'하면 진짜 breakeven
> 회사에까지 별도 순이익이 잘못 붙어 틀린 PER이 나온다. 결측이 틀린 값보다
> 낫다. 대상 2,578 중 이 구멍 추정 14건은 PER만 결측(PBR은 정상)으로
> 감수한다.

---

## 부록 A — 변경 이력(승인 사항)

### 2026-08-XX — 지표 테이블 확장(8종) + 볼린저밴드 오버레이 승인

**근거**: 앱인토스 채널톡 문의 답변 (수신일 **2026-08-XX — 정확한 날짜 확인
후 이 자리에 채워 넣을 것**, 원문 발췌는 사용자 제공):

> 문의주신 항목 검토 결과, 1번의 지표 6종과 2번의 볼린저밴드 차트 오버레이
> 모두 추가 가능합니다. 다만 지금까지 통과해오신 원칙 그대로, 아래를
> 유지해주셔야 합니다.
> - 모든 지표는 계산된 수치와 객관적 정의로만 표시 (예: '20일 평균에서 얼마나
>   벗어났는가')
> - '매수신호·과매수/과매도' 등 매매판단 표현, 자체 점수·등급·순위·추천
>   문구 미포함
> - 볼린저밴드는 밴드 선만 표시하고 매수·매도 시점 표식·문구는 추가하지 않기
> - 알림·푸시 미발송, 참고용 고지 및 데이터 출처 문구 유지

**정정(2026-08, 사용자 확인)**: 위 원문의 "1번의 지표 6종"에는 **볼린저 %B**도
포함되어 있었다 — 즉 원래 승인받은 지표는 RSI20·MFI·CCI·ADX·-DI·ATR·%B
**7종**이다. 구현 설계 단계에서 %B가 목록에서 누락되어 series 스키마와 화면
어느 쪽에도 반영되지 않은 채로 진행됐고, QR 테스트 중 사용자가 재확인해
`bpb` 필드와 지표 상세 행으로 추가했다(`pipeline/build_series.py`,
`src/components/ChartSheet.tsx`).

**추가 승인(2026-08, 앱인토스 상담원 재확인) — +DI**: 위 원문의 "1번의 지표
6종"에 +DI는 포함되지 않았고, 최초 설계 시점에는 승인 범위가 불명확해
`indicators.py`의 `adx()`가 +DI를 계산은 하되 의도적으로 버리고 -DI만
반환하도록 만들어 뒀었다. 이후 앱인토스 상담원에게 별도로 +DI 표시 가능
여부를 확인한 결과 "지표 자체(ADX 계산 과정에서 나오는 +DI)는 표시
가능"함이 확인되어, 계산 로직 변경 없이 반환값에 추가했다(`adx()`가
`(ADX, -DI)` 2-튜플 → `(ADX, +DI, -DI)` 3-튜플을 반환하도록 수정, `pdi`
필드 추가). 매매판단 문구 미포함 등 기존 조건은 동일하게 적용된다.

이로써 원래 6종 + %B(설계 누락 후 재추가) + +DI(상담원 재확인 후 추가) =
**총 8종**이 승인 지표 목록이다. 아래 표·목록의 "6종"·"7종"은 모두 "8종"으로
갱신한다.

**3장 원칙에 대한 개정 내용**:

| 기존 3장 원칙 | 개정 내용 |
|---|---|
| 차트에 볼린저밴드·이동평균 등 오버레이 절대 금지 | **밴드 선(20일 이동평균 ± 2 표준편차 상/하단)만 예외적으로 허용.** 매수·매도 마커/화살표/강조색은 여전히 금지. |
| (지표는 RSI(14) 1종만 존재) | RSI(20)·MFI(14)·CCI(20)·ADX·+DI·-DI·ATR·볼린저 %B 8종 추가 허용. 수치+객관적 정의만 표시, 매매판단 문구 금지 원칙은 그대로 유지. |
| 3-3 자체 점수·순위 금지 | 변경 없음 — 신규 지표에도 그대로 적용. |
| 3-4 데이터 출처·유의사항 표기 | 변경 없음 — 알림·푸시 미발송 조건이 이번에 다시 확인됨. |

**이번 승인 이후에도 그대로 유지되는 금지 사항(명시적으로 재확인됨)**:

- 매수/매도 마커·화살표·강조색
- "매수 신호", "과매수/과매도" 등 매매 판단 문구
- 자체 점수·등급·순위·추천 문구
- 알림·푸시 발송

**⚠️ 확인 필요 (사용자 보고용 — 임의로 처리하지 않음)**:

1. 정확한 수신 날짜 (현재 "2026-08-XX" placeholder 그대로 둠).
2. `src/lib/filters.ts:51,56`의 기존 "과매도"/"과열" 문구가 이번 승인 조건과
   정합적인지 — [3장 "관찰된 불일치"](#-관찰된-불일치-판단하지-않고-보고만-함)
   참고.

### 2026-08-11 — RSI(14)/(20)·MFI(14)/(20) 병기 추가

RSI(14)/(20) 병기 승인 전례에 근거, MFI(14)/(20) 병기도 동일 판단 적용하여
별도 문의 없이 추가함(2026-08-11, 운영자 직접 판단). UI는 개별 행이 아닌
상위 항목(RSI/MFI) 내 기간별 묶음 구조로 표시.

---

## 부록 B — SPEC 번호 없이 "검토 반려" 이력만 남은 항목 (참고)

`SPEC N장` 형식은 아니지만 동일한 성격(심사/규제 대응)의 결정이라 함께
기록합니다.

- **광고** — `src/components/BannerAd.tsx:4-9`: 2026-07-18 검토 반려 대응으로
  1차 출시 범위에서 제외. 사유: 인앱 광고는 사업자 등록이 필요한데 미등록
  상태라 실제 광고그룹 ID 발급 불가. 컴포넌트는 존재하되 App에서 사용하지
  않아 번들 미포함(tree-shake). 사업자 등록 후 복원 예정.
- **브랜드 정보** — `granite.config.ts:6,10`: 콘솔 등록 appName·아이콘과
  정확히 일치해야 함 (검토 반려 사유 #1, #3 대응).
- **튜토리얼 다크패턴 금지** — `src/components/TutorialSheet.tsx:14-19`:
  "진입 즉시 바텀시트"가 강제로 느껴지면 안 됨(앱인토스 심사 반려 사유) →
  닫기/건너뛰기 상시 노출, 딤 탭·뒤로가기로도 닫힘, 첫 1회만 자동 표시.

---

## 부록 C — 백로그: filters.ts RSI 슬라이더 문구("과매도"·"과열")

**출처**: 사용자 지시 (이 문서 검토 중 확정, 2026-08).

`src/lib/filters.ts:51,56`의 RSI 슬라이더 툴팁·눈금은 "과매도" · "과열"
표현을 쓰고 있다:

> 보통 30 아래를 과매도, 70 위를 과열이라고 부릅니다. (tip)
> ["0", "30 과매도", "50", "70 과열", "100"] (ticks)

**충돌 인지**: [부록 A](#부록-a--변경-이력승인-사항)의 2026-08 승인 조건 중
"'매수신호·과매수/과매도' 등 매매판단 표현 ... 미포함"과 원칙상 겹친다
([3장 "관찰된 불일치"](#-관찰된-불일치-판단하지-않고-보고만-함)에서 최초 보고).

**확정된 처리 방침(사용자 결정)**:

- 이 문구는 2026-08 승인 **이전부터** 존재했고, **이미 심사를 통과**했다.
- 따라서 이번 작업(지표 테이블 확장 + 볼린저밴드 오버레이) **범위 밖**이며
  지금은 손대지 않는다.
- 별도 검토 대상으로 **백로그**에 남긴다.
- 실제로 손대는 시점에는 반드시 **§9-6 "사전 공유" 절차**를 따를 것 — 이미
  통과한 문구를 건드리는 것 자체가 새로운 리스크를 여는 일일 수 있기 때문.

**⚠️ 문서 정합성 참고**: 이 SPEC.md는 리포 코드 주석에서 `grep`으로 역산해
재구성한 문서다(문서 맨 위 안내 참고). "§9-6"이나 9장을 가리키는 코드
주석·근거는 리포 어디에서도 찾지 못했다 — 즉 §9-6은 이 문서가 커버하는
범위(1~8장 + 부록 A/B/C) **밖**에 있는, 별도로 보관 중인 원본 SPEC의 절차로
보인다. 여기서는 "§9-6을 따를 것"이라는 지시만 그대로 기록해 두며, §9-6의
실제 내용은 추측해서 채우지 않는다. 원본 문서를 보관하고 계시다면 9장(과
그 이하 하위 절차 장)을 이 SPEC.md에 병합하는 것을 권장한다.

---

이 문서는 리포에 존재하지 않던 SPEC 원본을 코드 주석에서 역산해 재구성한
것입니다. 원본을 보관하고 계시다면 그것을 기준으로 이 문서의 장/절 번호와
문구를 교정해 주세요.
