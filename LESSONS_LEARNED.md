# LESSONS_LEARNED.md

> 리포에 "§11-2(틀린 가설 기록)"에 해당하는 원본 문서가 없습니다(SPEC.md와
> 같은 상황 — `§9-6`처럼 이 리포 밖에 있는 별도 플레이북의 절차로 보입니다).
> 이 파일은 그 §11-2 항목으로 옮겨 담을 수 있도록 준비한 요약이며, 아래
> 내용을 원본 플레이북의 §11-2에 그대로(또는 다듬어서) 옮겨 주세요.

## 2026-08 — ADX 계산 시 "RSI_WINDOW=30을 그대로 재사용해도 될 것"이라는 가설이 틀렸음

**세웠던 가설**: ADX/-DI도 RSI(14)처럼 Wilder 평활 기반이니, RSI가 이미
검증해 둔 `RSI_WINDOW=30`(2.14×period) 창 크기를 그대로 가져다 쓰면
충분할 것이다.

**틀렸다는 걸 확인한 방법**: 설계 단계에서는 "약 2×period면 충분하다"고
가정만 하고 넘어갔는데(구현 계획서에 "확인 필요"로만 표시), 구현 후
삼성전자(005930) 실측 데이터로 여러 창 크기를 직접 비교했다:

| window | ADX | -DI |
|---|---|---|
| 30 | 39.7 | 34.3 |
| 45 | 27.4 | 33.6 |
| 60 | 25.9 | 33.5 |
| 85 | 26.9 | 33.5 |

**결과**: `-DI`는 창 크기와 거의 무관하게 안정적이었다(33.1~35.2, 단일
Wilder 평활). 그런데 `ADX`는 `window=30`에서 39.7, `window=85`에서 26.9로
**47% 차이**가 났다 — RSI와 똑같이 생겼다고 해서 같은 burn-in으로
충분하지 않았다.

**원인**: RSI는 Wilder 평활을 **한 번**만 한다(gain/loss → RSI). ADX는
**두 번** 한다(DM/TR → DI, 그 다음 DX → ADX). 평활을 한 겹 더 씌울 때마다
"시드가 안정되기까지 걸리는 기간"이 곱으로 늘어나는데, 이걸 간과하고
"같은 계열 지표니 같은 창이면 되겠지"라고 넘겨짚었던 게 가설이 틀린
지점이다.

**고친 방법**: 이론적 최소치(`약 2×period`, 여기선 28)는 "값이 나오는"
조건이지 "값이 수렴하는" 조건이 아니라는 걸 확인했다. 여러 창 크기를
실측 비교해 `window≈45` 이상부터 24.4~27.4 범위로 수렴하는 걸 보고
`ADX_WINDOW=60`으로 확정했다(85와 비교해도 4% 미만 차이).

**다음에 적용할 일반 원칙**: 지표를 "같은 계열이니 같은 파라미터로
충분하다"고 유추만 하고 넘어가지 말 것. 특히 여러 단계의 평활·이동평균이
중첩된 지표(ADX처럼)는 단일 평활 지표(RSI·ATR)보다 burn-in이 훨씬 길 수
있으므로, 설계 단계의 가정은 반드시 실제 데이터로 여러 파라미터를
바꿔가며 비교 검증한 뒤 확정할 것 — "값이 반환된다(크래시 없음)"과 "값이
안정적이다"는 서로 다른 검증이다.

**근거 코드**: `pipeline/indicators.py`의 `adx()` 함수 docstring에 같은
실측 표와 설명을 남겨 뒀다(구현 시점에 확인한 채로 코드 옆에 고정).

## 2026-08 — "우선주 이름은 항상 '우'로 끝난다"는 가정이 틀렸음

**틀렸던 가정**: `fetch_prices.py`의 우선주 제외 정규식(`_PREF_RE`)을
`r"우[A-C]?$"`로 짜면서, "종목명이 '우' 또는 '우A/B/C'로 끝나면 우선주,
그 외 형태는 없다"고 가정했다(예시로 삼은 이름도 "삼성전자우", "하이트진로
2우B" 등 전부 끝이 '우'였다).

**실제 원인**: 전환우선주는 이름 끝에 `(전환)` 같은 괄호 접미사가 더
붙는다(`CJ4우(전환)`, `DL이앤씨2우(전환)`). `$`로 문자열 끝을 "우" 바로
뒤로 고정해 둔 탓에, "우" 뒤에 괄호가 더 붙는 순간 매칭이 깨졌다 — 우선주
'유형'이 하나가 아니라는 걸 놓친 것이 근본 원인이지, 정규식 문법 실수가
아니었다.

**발견 경위**: 지표 확장 작업 중 series 파일을 전수 검증하다가 비표준
코드(`00104K`, `37550L`) 두 개가 우연히 눈에 띄어 발견했다(볼린저밴드
작업과는 무관 — 우연히 걸린 부수 발견). 이번에 그 2건을 시작점 삼아
전종목(2,763종목, 필터 적용 전 원본 API 응답) 전수 스캔으로 범위를
확정했다: "우" 포함 종목 51개 중 실제 우선주 패턴은 정확히 이 2개뿐이었고,
나머지 49개는 "다우기술"·"우리금융지주"처럼 "우"가 들어간 일반 종목명이었다.

**수정**: `r"우[A-C]?(\([^)]*\))?$"`로 확장 — "우"(+옵션 A~C 클래스) 뒤에
괄호 접미사가 있어도 없어도 끝까지 매칭한다. 특정 키워드("전환")를
하드코딩하지 않고 괄호 안 내용은 임의로 허용했다 — "우선주 접미사가
'전환' 하나뿐"이라고 또 넘겨짚는 같은 실수를 반복하지 않기 위해서다.
수정 후 전종목 재검증: 새로 걸린 종목은 그 2개뿐(회귀 0, 과잉매칭 0).

**다음에 적용할 일반 원칙**: 정규식으로 "카테고리 판정"을 할 때, 손에 든
예시 몇 개로 패턴을 일반화하지 말 것. 특히 "끝에 오는 부가 표기"(우선주
유형, 스팩 회차 표기 등)는 예시에 없던 변형이 실제로 존재할 가능성이
높으므로, 전체 유니버스를 대상으로 "패턴에 걸리는 것"과 "안 걸리는 것"
양쪽을 다 훑어 확인한 뒤에 정규식을 확정할 것 — 이번처럼 "표본이 아니라
전수"가 실제로 새 케이스를 잡아냈다.
